# 프로젝트 메타 정보 (Project Meta)

> **이 문서의 역할** — 이 프로젝트를 세팅하면서 확정한 **사실과 결정의 단일 기록처**다.
> 코드에서 재도출할 수 없는 정보(계정·호스팅·결정 근거·자산 상태·설치 상태)만 담는다.
> 코드에서 확인 가능한 것(디렉토리 구조·의존성 버전)은 여기에 중복 기록하지 않는다.
>
> 최종 갱신: 2026-08-13

---

## 1. 저장소

| 항목           | 값                                                           |
| -------------- | ------------------------------------------------------------ |
| 원격 `origin`  | `physicalai-studio/physicalai-studio.github.io` — **public** |
| 배포 주소      | https://physicalai-studio.github.io/ (GitHub Pages, Actions) |
| 원격 `archive` | `DDUGGOGI/homepage` — private. 초기 상세 커밋 이력 보존용    |
| 기본 브랜치    | `main`                                                       |
| 로컬 클론 경로 | `/home/tr-omen-22/Documents/git/homepage`                    |
| git 협업 모드  | **solo** (`README.md` 에 선언 — git_workflow SOP §0)         |

**공개 범위 정책**: GitHub Pages 무료 요금제가 public 저장소만 호스팅하므로 공개 저장소를 쓴다.
공개 전 실명이 담긴 내부 문서(역량 대장)를 저장소 밖으로 옮기고 **이력을 새로 시작**했다 —
파일만 지우면 이전 커밋에 남기 때문이다. 상세 이력은 private `archive` 원격에만 있다.

**개인 저장소 정리**: `DDUGGOGI/dduggogi.github.io` 는 조직 이전 전에 잠시 쓰던 주소다.
GitHub 은 `<user>.github.io` 의 Pages 를 끌 수 없어, 새 주소로 넘기는 안내 페이지(noindex)만 남겼다.

---

## 2. 로컬 개발 환경

| 항목            | 값                                                                        | 확인일     |
| --------------- | ------------------------------------------------------------------------- | ---------- |
| OS              | Linux 6.8.0-136-generic                                                   | 2026-08-13 |
| Node.js         | v22.22.3                                                                  | 2026-08-13 |
| npm             | 10.9.8                                                                    | 2026-08-13 |
| GitHub CLI      | `gh`, `DDUGGOGI` 계정으로 인증됨 (HTTPS 프로토콜)                         | 2026-08-13 |
| git 사용자 정보 | **저장소 로컬 설정**: `user.name=DDUGGOGI`, `user.email=<GitHub noreply>` | 2026-08-13 |

> git 전역(global) 사용자 정보가 이 PC 에 설정되어 있지 않아 저장소 로컬로만 지정했다.
> 저장소가 공개이므로 개인 이메일 대신 **GitHub noreply 주소**를 쓴다 — 공개 커밋 이력은
> 스팸 수집 대상이 되기 때문이다. 다른 저장소에서 커밋하려면 동일 설정이 별도로 필요하다.

---

## 2-1. 연락처 · 대표자 (사이트 게시 정보)

사이트에 실제로 노출되는 정보다. 변경 시 `src/content/site.ts` · `company.ts` 한 곳만 고치면
푸터 · Contact 페이지 · About 이 함께 바뀐다.

| 항목   | 값               | 노출 위치             |
| ------ | ---------------- | --------------------- |
| 대표자 | 이다빈           | About — Founder       |
| 이메일 | selele@nate.com  | 푸터 · Contact 페이지 |
| 전화   | +82 10-7475-2889 | 푸터 · Contact 페이지 |

- `tel:` 링크는 `src/lib/contact.ts` 의 `telHref()` 가 숫자만 남겨 만든다 —
  표시용 하이픈이 들어간 문자열을 그대로 링크에 쓰면 일부 단말이 인식하지 못한다.
- **공개 사이트이므로 이 이메일·번호는 크롤러에 수집된다.** 스팸이 늘면 문의 폼(Phase 4,
  ADR-0004)으로 전환하고 직접 노출을 줄이는 것을 검토한다.

---

## 3. 확정된 결정 (Decision Log)

전략 문서 `개발_전략.md` §7의 ID 체계를 따른다. **확정 사항은 덮어쓰지 않고 supersede 로 기록**한다(coding SOP §6 로그-누적형).

### D2 — 호스팅: Cloudflare Pages ✅

- **확정일**: 2026-08-13
- **근거**: ① 무료 요금제에서 **상업적 이용 허용** ② 대역폭 여유(영상 중심 사이트에 유리) ③ 문의 폼용 서버리스 함수(Pages Functions) 제공 ④ 저장소 private 유지 가능
- **탈락 대안**:
  - Vercel — 개발 경험은 최상이나 무료(Hobby) 요금제가 **상업적 이용 불가**. 회사 홈페이지는 유료 전환 필요
  - GitHub Pages — 무료 요금제 기준 **저장소 public 필수** + 용량·대역폭 제한. 영상 중심 사이트에 부적합
- **재확인 필요**: 요금제·약관은 변경될 수 있다. **실제 배포 직전(Phase 5) 각 서비스 약관을 다시 확인**한다.

### D3 — 로케일: 단일 (영문 헤드라인 + 한국어 본문) ✅

- **확정일**: 2026-08-13
- **근거**: 디자인 컨셉 §6·§7의 카피 스타일(짧은 영문 대문자 헤딩 + 한국어 보조 문장)과 일치. 초기 타깃이 국내 산업·연구 고객
- **제약 조건(반드시 지킬 것)**: 모든 문구는 `src/content/` 데이터 모듈에만 존재한다. 컴포넌트 내부 문자열 하드코딩 금지 → 이후 영문판 추가 시 UI 코드 수정 0
- **미도입**: `[locale]` 라우트 세그먼트. 필요해지면 그때 도입한다

### D5 — 비주얼 자산: 보유 0, 신규 캡처 필요 ✅

- **확정일**: 2026-08-13
- **현황**: 사이트에 사용할 시뮬레이션 영상·스크린샷을 **현재 보유하고 있지 않다**
- **파급**: placeholder-first 아키텍처가 선택이 아니라 **전제**가 된다. Phase 0 의 "필요 미디어 자산 명세서"가 최우선 산출물
- **후속 작업**: 명세서에 따라 Isaac Sim 등에서 직접 캡처 → 캡처 track 은 코드 개발과 **병렬** 진행

---

## 4. 미결 결정

| ID      | 결정              | 필요 시점      | 비고                                                                                                              |
| ------- | ----------------- | -------------- | ----------------------------------------------------------------------------------------------------------------- |
| **D1**  | 브랜드명 · 도메인 | **Phase 1 전** | 로고·`<title>`·OG(Open Graph) 이미지·이메일 주소·JSON-LD 의 입력. 미확정 시 전 페이지 재작업 발생                 |
| ~~D2a~~ | 렌더링 모드       | —              | ✅ 확정 — 정적 export. [adr/0002-static-export.md](adr/0002-static-export.md)                                     |
| **D4**  | 문의 수신 경로    | Phase 4 전     | Pages Functions + 외부 이메일 API 조합. 발송 수단 확인 필요 — 과거 Workers 용 무료 MailChannels 연동은 종료되었다 |
| **D6**  | 사업자 정보 표기  | Phase 5 전     | 상호·대표자·연락처 등 footer 표기 범위                                                                            |

---

## 5. 기술 스택 (Phase 1 구현 완료)

| 층            | 선택                                                  | 상태                            |
| ------------- | ----------------------------------------------------- | ------------------------------- |
| Framework     | Next.js 16.3.0 (App Router, 정적 export) + TypeScript | ✅ 도입                         |
| UI            | React 19.2.8                                          | ✅ 도입                         |
| Styling       | Tailwind CSS 4 + 디자인 토큰(`src/styles/tokens.css`) | ✅ 도입                         |
| Animation     | CSS transition + IntersectionObserver (`Reveal`)      | ✅ 도입 (Framer Motion 미사용)  |
| Content       | `src/content/*.ts` + zod 4 스키마                     | ✅ 도입                         |
| 검증          | ESLint 9 · Prettier 3 · `tsc --noEmit` · Vitest 4     | ✅ 도입                         |
| 스모크 테스트 | Playwright                                            | ❌ 미도입 (debt-003)            |
| 포맷터 설정   | `.prettierrc.json` + `.editorconfig`                  | ✅ 생성                         |
| 폰트          | 시스템 폰트 스택                                      | ⚠ 한글 웹폰트 미도입 (debt-001) |

버전 근거와 라이선스·대안은 [adr/0006-frontend-dependencies.md](adr/0006-frontend-dependencies.md).

**배제 결정(의도적)**: 카드 중심 UI 킷(shadcn 등) — 디자인 컨셉 §17 "Rounded Card 금지"와 충돌. three.js 등 3D 렌더 라이브러리 — 가이드 §21 "의미 없는 3D Animation" 금지.

---

## 6. 하네스 / SOP 설치 상태

전역 `~/.claude/CLAUDE.md` 가 등록한 SOP 는 **대상 저장소에 번들이 설치되어야 활성화**된다(각 번들의 활성화 게이트 = `docs/claude_guideline/<번들>/` 존재).

| 번들                                            | 이 저장소 설치 상태  | 얻은 것                                           |
| ----------------------------------------------- | -------------------- | ------------------------------------------------- |
| `coding`                                        | ✅ 설치 (2026-08-13) | ADR 필드 검사, 금지 패턴 검사, 중복 시그니처 검사 |
| `git_workflow`                                  | ✅ 설치 (2026-08-13) | 세션 격리 staging 게이트                          |
| `code_review`                                   | ✅ 설치 (2026-08-13) | 인벤토리 양식, severity 평가 절차                 |
| `debt`                                          | ✅ 설치 (2026-08-13) | 부채 registry, TODO 마커 게이트                   |
| `issue_fix` · `mistake` · `user_instruction` 등 | ❌ 미설치            | —                                                 |

- 설치 원본: `/home/tr-omen-22/Documents/git/kuks_claude_agent_setup/<번들>/install.sh`
- CI 연결: `.github/workflows/ci.yml` 이 `adr-fields` · `debt-marker` · `banned-pattern` · `dup-signature` 를 실행한다.

### 검사 스크립트의 실제 강제 범위 (정직 기록)

번들 검사기는 Node.js 프로젝트를 전제로 만들어지지 않아 다음 한계가 있다. 우회하지 않고 그대로 기록한다.

| 검사                | 한계                                                                                                      | 대응                                        |
| ------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| `debt-marker.sh`    | `--include` 목록에 `*.tsx` 가 없어 **React 컴포넌트의 TODO 는 검사되지 않는다.** `node_modules` 도 미제외 | 대상을 `src` 로 좁혀 실행. 한계는 잔존      |
| `banned-pattern.sh` | 빌드 산출물(`out/`·`.next/`)의 번들 JS 를 스캔하면 대량 오탐                                              | CI 에서 **빌드 이전에**, 대상 `src` 로 실행 |
| `format.sh`         | Prettier 를 인식하지 못해 "포맷터 없음"으로 검사를 생략한다                                               | 포맷 강제는 `npm run format:check` 가 담당  |

---

## 7. 미디어 자산 상태

| 항목           | 상태                                                                                                                              |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 보유 자산      | Isaac Sim 캡처 2장(자체) → 6슬롯 + 자유 라이선스 이미지 4장(제3자, 초안) → 4슬롯. 영상은 **아직 없음**                            |
| 저장 정책      | 원본 영상은 **git 에 커밋하지 않는다.** 외부 스토리지/CDN(Content Delivery Network) 에 두고 `src/content/media.ts` 가 URL 만 참조 |
| 저장소 위치    | 정지 이미지는 `public/media/`(WebP, 총 400KB). **영상은 외부 스토리지** — 미정 (Cloudflare R2 등)                                 |
| 원본 보관      | `image/` (PNG 원본). 크롭·변환 스크립트는 세션 스크래치패드에 있었고 저장소에 남기지 않았다                                       |
| 명세서         | ✅ [미디어_자산_명세서.md](미디어_자산_명세서.md) — 캡처·인코딩 규격, 슬롯 9종 요구 자산                                          |
| 구현           | ✅ `MediaSlot` 3단 폴백 + `clearance` 필드로 미확인 자산 게시 차단                                                                |
| 공개 근거 기록 | `self-produced` / `client-approved` / `anonymized` / `pending`. `pending` 은 코드가 게시하지 않음                                 |

---

## 8. 보안 메모

- **2026-08-13**: GitHub PAT(Personal Access Token) 1건이 평문으로 채팅에 노출됨. 이 프로젝트에서 사용하지 않았고 어떤 파일에도 저장하지 않았으나, **해당 토큰은 폐기(revoke)해야 한다** → https://github.com/settings/tokens . 인증은 `gh` CLI 세션을 사용한다.
- 이 저장소에는 토큰·키·`.env` 를 커밋하지 않는다. `.gitignore` 에 `.env*`·`*.pem`·`*.key` 등록 완료.

---

## 9. 세션 이력

| 일자       | 내용                                                                                                                                          | 산출물                                                                                   |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 2026-08-13 | 저장소 생성·클론, 초기 커밋                                                                                                                   | `README.md`, `.gitignore`                                                                |
| 2026-08-13 | 가이드 문서 분석 → 개발 전략 수립, 결정 D2·D3·D5 확정                                                                                         | `docs/개발_전략.md`, `docs/프로젝트_메타.md`                                             |
| 2026-08-13 | **Phase 0** — 하네스 4종 설치, ADR 6건, 미디어 자산 명세서, 부채 registry                                                                     | `docs/adr/`, `docs/미디어_자산_명세서.md`, `docs/debt/registry.md`                       |
| 2026-08-13 | **Phase 1** — Next.js 기반 구축. 6개 라우트 · 디자인 토큰 · 콘텐츠 계층 · MediaSlot · CI                                                      | `src/`, `.github/workflows/ci.yml`, `docs/code_inventory/2026-08-13.md`                  |
| 2026-08-13 | `npm run dev` 크래시 진단(inotify 한도) → 기록. 실행 가이드 작성                                                                              | `docs/issues_and_fixes/issues_and_fixes.md`, `cmd.md`                                    |
| 2026-08-13 | 이미지 투입 — 자체 캡처 2장 크롭 → 6슬롯, CC0 1장 → 1슬롯. `licensed` clearance + 출처 표기 강제                                              | `public/media/`, `src/content/media.ts`, `src/content/schema.ts`                         |
| 2026-08-13 | 홈 확장 — Hero 아래 7개 섹션 추가, Hero 카피를 content 계층으로 이관                                                                          | `src/content/home.ts`, `src/app/page.tsx`                                                |
| 2026-08-13 | my_docs 업무 기록 → 역량 지도 정리, 사이트에 역량 수준으로 반영(고객사·실측치 제외)                                                           | 역량 대장(저장소 밖 비공개), `src/content/{technology,capabilities,services,company}.ts` |
| 2026-08-13 | **사업 정의 확정**(ADR-0008) — 사전 검증 + Sim-to-Real 정합 루프. 서비스 5종 재편, 워크플로를 루프로                                          | `docs/사업_정의.md`, `src/content/{site,home,services,technology,capabilities}.ts`       |
| 2026-08-13 | 비주얼 컨셉 확정 — 파동·간섭 형태 언어(ADR-0007). 형태 4종 제한, 개념 대응표 고정                                                             | `docs/비주얼_컨셉_파동과_간섭.md`, `docs/adr/0007-*.md`                                  |
| 2026-08-13 | 배경 광장 2종 절차적 생성(고리·간섭 무늬), 초록 듀오톤 그레이딩 도입                                                                          | `scripts/gen-fields.py`, `scripts/build-media.py`                                        |
| 2026-08-13 | 제3자 자유 라이선스 이미지 3장 추가(포인트 클라우드 · 액추에이터 · 휴머노이드) → Technology · About 에 비주얼 신설. CC BY 변경 사실 표시 대응 | `image/`, `public/media/`, `src/app/technology`, `src/app/about`                         |

---

## 10. 이 문서의 갱신 규칙

- **결정이 확정될 때마다** §3 에 근거·확정일과 함께 추가하고 §4 에서 제거한다.
- **기존 결정을 뒤집을 때**는 항목을 지우지 말고 `Status: Superseded by <새 ID>` 를 붙여 남긴다(결정의 역사가 근거다).
- 환경 값(§2)은 변경 시 확인일과 함께 갱신한다.
- 세션이 끝날 때 §9 에 한 줄 추가한다.
